-- JUMPSCARE

local JumpscareGui = Instance.new("ScreenGui")
JumpscareGui.Parent = game.CoreGui
JumpscareGui.IgnoreGuiInset = true

local Image = Instance.new("ImageLabel")
Image.Parent = JumpscareGui
Image.Size = UDim2.new(1,0,1,0)
Image.BackgroundTransparency = 1
Image.Image = "rbxassetid://140686756867361"
Image.ImageTransparency = 1

local Sound = Instance.new("Sound")
Sound.Parent = workspace
Sound.SoundId = "rbxassetid://5410086218"
Sound.Volume = 10
Sound:Play()

for i = 1,20 do
	Image.ImageTransparency = 1 - (i/20)
	task.wait(0.02)
end

task.wait(2)

for i = 1,20 do
	Image.ImageTransparency = i/20
	task.wait(0.02)
end

JumpscareGui:Destroy()
Sound:Destroy()

--// SIGMA DREAM GUI

local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local DreamState = Instance.new("TextButton")
local GetDream = Instance.new("TextButton")

ScreenGui.Parent = game.CoreGui
ScreenGui.Name = "SigmaDream"

Frame.Parent = ScreenGui
Frame.Size = UDim2.new(0,170,0,100)
Frame.Position = UDim2.new(0.42,0,0.4,0)
Frame.BorderSizePixel = 0
Frame.Active = true
Frame.Draggable = true

local FrameCorner = Instance.new("UICorner", Frame)
FrameCorner.CornerRadius = UDim.new(0,10)

-- RGB FRAME
task.spawn(function()
	while true do
		for i = 0,255,2 do
			Frame.BackgroundColor3 = Color3.fromHSV(i/255,1,1)
			task.wait()
		end
	end
end)

-- CLICK ONE TIME BUTTON
DreamState.Parent = Frame
DreamState.Size = UDim2.new(0,145,0,30)
DreamState.Position = UDim2.new(0.07,0,0.12,0)
DreamState.BackgroundColor3 = Color3.fromRGB(255,255,255)
DreamState.BorderSizePixel = 0
DreamState.Text = "Click One Time"
DreamState.TextColor3 = Color3.fromRGB(0,0,0)
DreamState.Font = Enum.Font.GothamBold
DreamState.TextSize = 14

local Corner1 = Instance.new("UICorner", DreamState)
Corner1.CornerRadius = UDim.new(0,8)

-- GET BRAINROTS TOGGLE
GetDream.Parent = Frame
GetDream.Size = UDim2.new(0,145,0,30)
GetDream.Position = UDim2.new(0.07,0,0.55,0)
GetDream.BackgroundColor3 = Color3.fromRGB(255,255,255)
GetDream.BorderSizePixel = 0
GetDream.Text = "Get Brainrots : OFF"
GetDream.TextColor3 = Color3.fromRGB(0,0,0)
GetDream.Font = Enum.Font.GothamBold
GetDream.TextSize = 13

local Corner2 = Instance.new("UICorner", GetDream)
Corner2.CornerRadius = UDim.new(0,8)

-- CLICK ONE TIME FUNCTION
DreamState.MouseButton1Click:Connect(function()
	game:GetService("ReplicatedStorage")
		:WaitForChild("Remotes")
		:WaitForChild("DreamStateChanged")
		:FireServer(true)
end)

-- TOGGLE
local Toggle = false

GetDream.MouseButton1Click:Connect(function()
	Toggle = not Toggle

	if Toggle then
		GetDream.Text = "Get Brainrots : ON"
	else
		GetDream.Text = "Get Brainrots : OFF"
	end
end)

-- SUPER FAST GET BRAINROTS
task.spawn(function()
	while true do
		if Toggle then

			game:GetService("ReplicatedStorage")
				:WaitForChild("Remotes")
				:WaitForChild("RequestDreamBrainrots")
				:FireServer()

			game:GetService("ReplicatedStorage")
				:WaitForChild("Remotes")
				:WaitForChild("PickupDreamBrainrot")
				:FireServer("59")

			game:GetService("ReplicatedStorage")
				:WaitForChild("Remotes")
				:WaitForChild("RequestDreamWallExit")
				:FireServer()

		end

		task.wait(0.05)
	end
end)
